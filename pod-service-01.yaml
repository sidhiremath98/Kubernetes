apiVersion: v1
kind: Pod
metadata:
  name: nginx-pod
  namespace: dev-namespace
  labels:
    app: nginx
    component: web
    tier: frontend
    environment: dev
    project: eksctl-cluster-development
spec:
  containers: 
  - name: nginx
    image: nginx
    ports:
    - containerPort: 8080